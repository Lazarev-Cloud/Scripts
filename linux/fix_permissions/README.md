# Fix Permissions

Corrects ownership and permission issues in the current user's home directory.

## Usage

```bash
sudo ./fix_permissions.sh
```

Logs are written to `/var/log/fix_permissions.log`.
